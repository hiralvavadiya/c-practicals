//convert lower case to upper case :-nope
#include<stdio.h>
int main (){
    char a[100],i,cnt =0;
    printf("enter a =");
    fgets(a,100,stdin);
    for(i=0;i<n;i++)
    {
        if(a[i]>65 && a[i]<=90)
        {
            cnt++;
        }
    }
    printf("%d",cnt);
    
    int val = puts(a);

    return 0;
}